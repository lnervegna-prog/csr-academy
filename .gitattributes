# Normalise to LF in the repo so Netlify and any Linux tooling see clean files.
* text=auto eol=lf

# Windows batch files must keep CRLF or cmd.exe misreads them.
*.cmd text eol=crlf
*.bat text eol=crlf
